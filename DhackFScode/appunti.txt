montare nuovofs da prompt:
accedere a cartella di lavoro:   cd percorso
esecuzione:                      python filesystem.py  Mountpoint:   es. X:

X: disco su cui viene montato

Le informazioni relative a mountpount vengono lette da linea di comando, che il main può salvare all'interno
di variabili grazie al metodo ArgumentParser (ultimo blocco).

aggiungere metadata ai parametri di vfilesys


 
    